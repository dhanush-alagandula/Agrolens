<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <link rel="stylesheet" href="/css/login_page.css">
    <link
        href="https://fonts.googleapis.com/css2?family=Anek+Kannada:wght@100..800&family=DM+Sans:ital,opsz,wght@0,9..40,100..1000;1,9..40,100..1000&family=Urbanist:ital,wght@0,100..900;1,100..900&display=swap"
        rel="stylesheet">
    <title>Login Page</title>
    <style>
        * {
            margin: 0;
            padding: 0;
            box-sizing: border-box;
            font-family: "Urbanist", sans-serif;
        }

        body {
            font-family: "Urbanist", sans-serif;
            height: 100vh;
            display: flex;
            justify-content: center;
            align-items: center;
            background: url('https://res.cloudinary.com/dl0thhfbc/image/upload/v1731652188/kp2jiolbcxtn5kmrmj0t.png') no-repeat center center;
            background-size: cover;
        }

        .left-side {
            display: flex;
            justify-content: center;
            align-items: center;
            flex: 1;
            transition: all 0.3s ease;
        }

        .logo {
            position: relative;
            left: 100px;
            width: 340px;
            height: 270px;
            transition: transform 1.3s ease;
        }

        .logo:hover {
            transform: rotateY(360deg) scale(1.2);
            /* 3D spin effect with scaling */
        }

        .right-side {
            flex: 1;
            padding: 20px;
            display: flex;
            justify-content: center;
            align-items: center;
            height: 100%;
        }

        .signin {
            font-family: "Urbanist", sans-serif;
            background: rgba(255, 255, 255, 0.9);
            border-radius: 10px;
            padding: 30px;
            width: 100%;
            max-width: 400px;
            box-shadow: 0 4px 8px rgba(0, 0, 0, 0.1);
            animation: slideIn 1s ease-out;
            margin-right: 100px;
        }

        h2 {
            font-family: "Urbanist", sans-serif;
            text-align: center;
            font-weight: 600;
            color: #333;
            margin-bottom: 20px;
        }

        label {
            display: block;
            margin-bottom: 15px;
        }

        p {
            font-size: 16px;
            color: #333;
        }

        input {
            width: 100%;
            padding: 12px;
            margin-top: 5px;
            border: 1px solid #ccc;
            border-radius: 5px;
            font-size: 16px;
            transition: border-color 0.3s;
        }

        input:focus {
            border-color: #4CAF50;
            outline: none;
        }

        .forgot {
            align-items: center;
            text-align: center;
            margin-top: 10px;
            color: #555;
            font-size: 14px;
        }

        .submit button {
            font-family: "Urbanist", sans-serif;
            width: 100%;
            padding: 12px;
            align-items: center;
            text-align: center;
            background-color: #4CAF50;
            margin-top: 10px;
            color: white;
            border: none;
            border-radius: 5px;
            font-size: 18px;
            cursor: pointer;
            transition: background-color 0.3s;
        }

        .forgot a:hover {
            color: rgb(255, 255, 255);
        }

        .submit button:hover {
            background-color: #45a049;
        }

        .f2 {
            text-align: center;
            margin-top: 20px;
        }

        .f2 p pre {
            font-size: 16px;
            color: #999;
        }

        .google-signin {
            display: flex;
            align-items: center;
            justify-content: center;
            background-color: transparent;
            color: black;
            padding: 10px 20px;
            border-radius: 5px;
            font-size: 16px;
            cursor: pointer;
            transition: background-color 0.3s;
            margin-top: 10px;
            text-decoration: none;
        }

        .google-signin img {
            width: 20px;
            height: 20px;
            margin-right: 10px;
        }

        .google-signin:hover {
            background-color: #f9fbff;
        }

        /* Animation for sliding the sign-in box */
        @keyframes slideIn {
            from {
                transform: translateX(50%);
                opacity: 0;
            }

            to {
                transform: translateX(0);
                opacity: 1;
            }
        }

        /* Responsive styling */
        @media (max-width: 768px) {
            body {
                background: #f4f4f4;
                /* Simple background on mobile */
            }

            .left-side {
                display: none;
                /* Hide logo on smaller screens */
            }

            .right-side {
                flex: 1 1 100%;
                /* Full width on mobile */
            }

            .signin {
                padding: 20px;
                width: 100%;
            }
        }
    </style>
</head>

<body>
    <div class="left-side">
        <img src="https://res.cloudinary.com/dl0thhfbc/image/upload/v1731612674/de1gwhutgwjjg2jgpy3g.png" alt="Logo"
            class="logo">
    </div>

    <div class="right-side">
        <form action="/login" method="post">
            <div class="signin">
                <h2>Login to Agrolens</h2>
                <label for="email">
                    <p>E-mail</p>
                    <input type="email" id="email" name="email" placeholder="Example@gmail.com" required>
                </label>
                <label for="password">
                    <p>Password</p>
                    <input type="password" id="password" name="password" placeholder="Password">
                </label>
                <a href="/forgot/email" class="forgot">Forgot Password?</a>
                <% if (error) { %>
                    <p style="color: red; text-align: center; font-size: 14px; margin-top: 10px;">
                        <%= error %>
                    </p>
                    <% } %>
                        <div class="submit">
                            <button type="submit">Submit</button>
                        </div>
                        <div class="f2">
                            <p>
                            <pre>—————————or————————</pre>
                            </p>
                            <a href="/auth/google" class="google-signin">
                                <img src="https://img.icons8.com/?size=100&id=V5cGWnc9R4xj&format=png&color=000000"
                                    alt="Google Icon"> Sign in with Google
                            </a>
                            <a href="/signup" class="forgot">Don't have an account? <b>Signup now</b></a>
                        </div>
            </div>
        </form>
    </div>
</body>

</html>